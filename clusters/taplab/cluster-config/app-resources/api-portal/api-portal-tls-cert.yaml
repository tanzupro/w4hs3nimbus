apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: api-portal-tls-cert.yaml
  namespace: api-portal
spec:
  dnsNames:
  - api-portal.w4hs3nimbus.tanzu.pro
  secretName: api-portal-tls-cert
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
